<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Gaming Shop App</title>
    </head>
<body>
  <%- include ('partials/navbar.ejs') %>
  <% if (login[0].login_status === 1) { %>
    <div class="container">
        <br>
        <div class="text-center"><h2>Update Item</h2></div>
        <br>
        <div class="text-center">
          <% if (items) { %> 
            <form action="/editItem/<%= items.itemId %>" method="POST" enctype="multipart/form-data">
                <label for="name">Item Name:</label><br>
                <input type="text" id="name" name="name" value = "<%=items.name%>" required><br><br>

                <label for="name">Item Description:</label><br>
                <input type="text" id="i_description" name="i_description" value = "<%= items.i_description %>" required><br><br>

                <label for="quantity">Quantity:</label><br>
                <input type="number" id="quantity" name="quantity" min="0" step="1" value = "<%= items.quantity %>" required><br><br>

                <label for="price">Price:</label><br>
                <input type="number" id="price" name="price" min="0" step="0.01" value = "<%= items.price %>" required><br><br>

                <label for="image">Current Image:</label><br>
                <input type="text" name="currentImage" value = "<%= items.image %>" disabled><br>
                <img src = "/images/<%= items.image %>" width="20%"><br><br>

                New Image:<br> <input type="file" id="image" name="image" accept="image/*" value = "<%=items.image%>"><br><br>

                <button type="submit">Update Item</button>
            </form>
          <% } else { %>
              <p>No item found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
    <% } else { %> 
        <div class="container">
            <br>
            <div class="text-center"><h2>Access Denied</h2></div>
            <br>
            <div class="text-center">
                <h3>You must be logged in to update an item.</h3>
                <a href="/login">Login</a>
            </div>
        </div>
    <% } %>
</body>
</html>
